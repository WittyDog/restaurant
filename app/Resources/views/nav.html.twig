<nav class="navbar navbar-inverse">
    <div class="container-fluid">
        <div class="navbar-header">
            <a class="navbar-brand" href="{{ path('index') }}">Accueil</a>
        </div>
        {% if is_granted('ROLE_SERVEUR') %}
            <ul class="nav navbar-nav">
                <li><a href="{{ path('utilisateur_index') }}">Utilisateurs</a></li>
                <li><a href="{{ path('menu_index') }}">Menus</a></li>
                <li><a href="{{ path('plat_index') }}">Plats</a></li>
                <li><a href="{{ path('reservation_index') }}">Réservations</a></li>
            </ul>
        {% else %}
            <ul class="nav navbar-nav">
                <li><a href="{{ path('menu_index') }}">Menus</a></li>
                <li><a href="{{ path('plat_index') }}">Plats</a></li>
                <li><a href="{{ path('reservation_new') }}">Faire une réservation</a></li>
            </ul>
        {% endif %}

        <ul class="nav navbar-nav navbar-right">
            {% if not app.user %}
                <li><a href="{{ path('utilisateur_new') }}"><span class="glyphicon glyphicon-user"></span> S'enregistrer</a></li>
                <li><a href="{{ path('login') }}"><span class="glyphicon glyphicon-log-in"></span> Connexion</a></li>
            {% else %}
                <li><a href="{{ path('utilisateur_show', { 'id': app.user.id }) }}"><span class="glyphicon glyphicon-user"></span> {{ app.user.prenom }} {{ app.user.nom }}</a></li>
                <li><a href="{{ path('logout') }}"><span class="glyphicon glyphicon-log-out"></span> Deconnexion</a></li>
            {% endif %}
        </ul>
    </div>
</nav>